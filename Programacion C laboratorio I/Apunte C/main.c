#include <stdio.h> // librerias, sirven para tener el codigo de las funciones q llamamos, printf, scanf, etc
#include <stdlib.h> // standard input output, entrada y salida teclado y monitor.

int main()  // funcion principal del programa a ejecutar
{

DECLARACION DE VARIABLES:
int ( variable entera )
char (caracter)
float (con ,)

mascaras:
int %d
float %f
char %c

%2f = podes indicar la cantidad de decimales q queremos;

"f"= cadena de caracteres.
char nombre [20], cadena de caracteres %s sin &.

para imprimir es de izq a derecha ej: printf("el numero 1 %d, el numero2 %d, el numero 3%d",numero1,numero2,numero3);


promedio= (float) (numero1+numero2+numero3)/3 ; // esto es castear



printf=("\n")//genera un salto de linea en el programa.


return=0 //devuelve un entero

}
